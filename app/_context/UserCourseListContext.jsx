import { createContext } from "react";

// to get the no.of courses created by user to set the progress and no.of courses in the sidebar and hence to make like saas. after 5 courses,
// make it like only do more after paying.
export const UserCourseListContext = createContext();
